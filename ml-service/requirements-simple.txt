# Simplified requirements without heavy dependencies
# Use pre-built wheels compatible with Python 3.13

Flask==3.0.0
flask-cors==4.0.0
requests==2.31.0

# Use newer versions with pre-built wheels for Python 3.13
numpy>=1.26.0
pandas>=2.1.0
