# 📱 AIAccounter Mini App Frontend

Telegram Mini App интерфейс для AIAccounter.

## 📦 Структура

```
miniapp/
├── index.html          # Главная страница приложения
├── login.html          # Страница входа
├── setup.html          # Инструкции по настройке
├── style.css           # Стили приложения
├── app.js              # Основная логика
├── api-helper.js       # API клиент
├── miniapp-config.js   # Конфигурация
└── server.py           # Локальный dev сервер (для разработки)
```

## 🚀 Деплой на Cloudflare Pages

### Быстрый старт

1. **Зайдите на** https://dash.cloudflare.com
2. **Pages → Create a project → Connect to Git**
3. **Выберите репозиторий** AIAccounter

### Настройки сборки

```yaml
Framework preset: None  # НЕ Next.js!
Build command: (пусто)
Build output directory: miniapp
Root directory: (пусто)
Branch: main
```

**⚠️ Важно**: Cloudflare может автоматически определить проект как Next.js из-за других файлов в репозитории. Обязательно измените на "None"!

### После деплоя

1. Получите URL (например: `https://aiaccounter.pages.dev`)
2. Обновите `miniapp-config.js` с Railway backend URL
3. Коммит и пуш
4. Cloudflare автоматически пересоберет

## 🛠️ Локальная разработка

### Вариант 1: Live Server (рекомендуется)

```bash
# VS Code extension: Live Server
# Или любой другой статический сервер
```

### Вариант 2: Python сервер

```bash
python server.py
# Откройте http://127.0.0.1:5500
```

### Вариант 3: Python встроенный

```bash
python -m http.server 5500
# Откройте http://127.0.0.1:5500
```

## 🔧 Конфигурация

Файл `miniapp-config.js` содержит настройки:

```javascript
const MINIAPP_CONFIG = {
    api: {
        baseUrl: window.location.hostname === 'localhost'
            ? 'http://localhost:8000/api/v1'
            : 'https://your-railway-app.up.railway.app/api/v1',
        enabled: true
    },
    defaultCurrency: 'KGS',
    timezone: 'Asia/Bishkek',
    language: 'ru'
};
```

**После деплоя backend на Railway** замените `your-railway-app.up.railway.app` на ваш реальный URL.

## 📝 Особенности

- ✅ Статические HTML/CSS/JS файлы
- ✅ Не требует сборки
- ✅ Работает в Telegram WebApp
- ✅ Автоматически переключается между dev/prod
- ✅ Поддержка темной темы Telegram

## 🔗 Связанные файлы

- Backend API: `../backend/`
- Документация: `../docs/`
- Деплой инструкция: `../DEPLOY.md`
- Чеклист деплоя: `../DEPLOY_CHECKLIST.md`
