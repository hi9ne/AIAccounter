# ✅ Чеклист для работы над AIAccounter

**Дата:** 21 ноября 2025  
**Текущая версия:** v2.4.5 / Frontend v3.0.3  

---

## 🎯 ПРИОРИТЕТНЫЕ ЗАДАЧИ

### 🔴 Критические (Сделать ASAP)

- [ ] **Проверить деплой на Railway** - убедиться что backend работает
- [ ] **Проверить Cloudflare Pages** - фронтенд доступен
- [ ] **Тестовый запрос через Mini App** - авторизация работает
- [ ] **Проверить Supabase БД** - соединение активно

### 🟡 Важные (В ближайшее время)

- [ ] **Обновить .env** файл с актуальными credentials
- [ ] **Протестировать все API эндпоинты** через Swagger
- [ ] **Проверить n8n workflows** - все активны
- [ ] **Тест Telegram бота** - отправить команды

### 🟢 Улучшения (Можно отложить)

- [ ] Добавить rate limiting на API
- [ ] Улучшить кэширование с Redis
- [ ] Добавить WebSocket для real-time
- [ ] Реализовать push уведомления

---

## 🔧 БЫСТРАЯ ПРОВЕРКА СИСТЕМЫ

### 1. Backend (FastAPI)

```bash
# Проверка health
curl https://aiaccounterbackend-production.up.railway.app/health

# Ожидаемый ответ:
{
  "status": "healthy",
  "service": "AIAccounter API"
}
```

### 2. Database (Supabase)

```sql
-- Проверка пользователей
SELECT COUNT(*) as total_users FROM users WHERE is_active = true;

-- Проверка транзакций за сегодня
SELECT COUNT(*) as today_transactions 
FROM expenses 
WHERE date >= CURRENT_DATE 
  AND deleted_at IS NULL;
```

### 3. Frontend (Mini App)

```
Открыть: https://aiaccounter.pages.dev
1. Должна загрузиться страница
2. Авторизация через Telegram
3. Главная страница с балансом
```

### 4. n8n Workflows

```
Зайти: https://hi9neee.app.n8n.cloud
Проверить статус:
  ✅ Ai Financer - ACTIVE
  ✅ Helper AI Financer - ACTIVE
  ✅ ExchangeRates_Daily - ACTIVE
  ✅ Recurring_Payments_Checker - ACTIVE
```

---

## 📋 ДЕТАЛЬНЫЙ ЧЕКЛИСТ ПО КОМПОНЕНТАМ

### Backend API

#### Endpoints проверить:

- [ ] `GET /health` - работает
- [ ] `POST /api/v1/auth/telegram` - авторизация
- [ ] `GET /api/v1/auth/me` - получение юзера
- [ ] `GET /api/v1/analytics/stats` - статистика
- [ ] `GET /api/v1/analytics/overview` - комплексный обзор
- [ ] `GET /api/v1/analytics/categories/top` - топ категории
- [ ] `GET /api/v1/expenses` - список расходов
- [ ] `POST /api/v1/expenses` - создание расхода
- [ ] `DELETE /api/v1/expenses/{id}` - удаление
- [ ] `GET /api/v1/income` - список доходов
- [ ] `GET /api/v1/rates/latest` - курсы валют
- [ ] `GET /api/v1/budget/status` - статус бюджета
- [ ] `GET /api/v1/reports/period` - отчёт

#### Логи проверить:

```bash
# Railway Dashboard → View Logs
# Искать:
✅ "Server started"
✅ "✅ [AUTH] Authentication successful"
❌ "❌ Error"
❌ Exception traces
```

---

### Frontend Mini App

#### Страницы проверить:

- [ ] **🏠 Главная (Home)**
  - [ ] Баланс отображается
  - [ ] Доходы/расходы корректны
  - [ ] Последние транзакции загружаются
  - [ ] Топ категории показываются
  - [ ] Периоды переключаются (день/неделя/месяц/год)

- [ ] **📊 Аналитика (Analytics)**
  - [ ] График баланса рисуется
  - [ ] Pie chart категорий работает
  - [ ] Тренды отображаются
  - [ ] Экспорт работает

- [ ] **📜 История (History)**
  - [ ] Список транзакций загружается
  - [ ] Фильтры работают (доходы/расходы)
  - [ ] Поиск работает
  - [ ] Infinite scroll работает

- [ ] **⚙️ Настройки (Settings)**
  - [ ] Смена темы работает
  - [ ] Смена валюты работает
  - [ ] Инфо пользователя отображается

- [ ] **📄 Отчёты (Reports)**
  - [ ] Генерация отчётов работает
  - [ ] CSV экспорт работает
  - [ ] Excel экспорт работает

#### Функционал проверить:

- [ ] **Авторизация**
  - [ ] JWT токен сохраняется
  - [ ] Автологин при повторном визите
  - [ ] Logout очищает токен

- [ ] **Кэширование**
  - [ ] Данные кэшируются (проверить Network tab)
  - [ ] TTL работает (5 минут)
  - [ ] Cache invalidation при новой версии

- [ ] **Offline режим**
  - [ ] Service Worker регистрируется
  - [ ] Страница работает offline
  - [ ] API fallback на cache

- [ ] **Responsive**
  - [ ] Мобильный вид (< 768px)
  - [ ] Планшет (768px - 1024px)
  - [ ] Десктоп (> 1024px)

---

### База данных

#### Таблицы проверить:

- [ ] `users` - есть записи
- [ ] `expenses` - есть данные
- [ ] `income` - есть данные
- [ ] `budgets` - настроены
- [ ] `exchange_rates` - курсы актуальны
- [ ] `notifications` - уведомления создаются
- [ ] `recurring_payments` - подписки есть
- [ ] `expenses_embeddings` - векторы генерируются
- [ ] `income_embeddings` - векторы генерируются

#### Функции проверить:

```sql
-- RAG поиск расходов
SELECT * FROM match_expenses_documents(
  '[0.1, 0.2, ...]'::vector, -- тестовый embedding
  5,                          -- top 5
  1                           -- user_id
);

-- RAG поиск доходов
SELECT * FROM match_income_documents(
  '[0.1, 0.2, ...]'::vector,
  5,
  1
);
```

#### Индексы проверить:

```sql
-- Список индексов
SELECT tablename, indexname, indexdef 
FROM pg_indexes 
WHERE schemaname = 'public'
ORDER BY tablename, indexname;
```

---

### n8n Workflows

#### Ai Financer проверить:

- [ ] Webhook URL активен
- [ ] Telegram триггер работает
- [ ] Голосовой ввод работает (Whisper)
- [ ] Main AI Agent отвечает
- [ ] Income Agent работает
- [ ] Expenses Agent работает
- [ ] RAG поиск работает
- [ ] CRUD операции работают
- [ ] Chat memory сохраняется

#### Helper AI Financer проверить:

- [ ] Workflow ID настроен в главном
- [ ] Call работает корректно

#### ExchangeRates_Daily проверить:

- [ ] Cron trigger: `0 9 * * *` (09:00)
- [ ] Запрос к API курсов
- [ ] Сохранение в БД
- [ ] Логи проверить

#### Recurring_Payments_Checker проверить:

- [ ] Cron trigger: `0 9 * * *` (09:00)
- [ ] Выборка подписок из БД
- [ ] Проверка дат
- [ ] Отправка уведомлений

---

## 🔍 ТЕСТОВЫЕ СЦЕНАРИИ

### Сценарий 1: Новый пользователь

```
1. Открыть Mini App в Telegram
2. Первый запуск → авторизация
3. Главная страница → "0 с" баланс
4. Перейти в Настройки → инфо отображается
5. Вернуться на Главную
```

### Сценарий 2: Добавление расхода через бота

```
1. Отправить боту: "Потратил 500 сом на еду"
2. Бот распознаёт категорию "Еда"
3. Сохраняет в БД
4. Отправляет подтверждение
5. Открыть Mini App → расход в истории
```

### Сценарий 3: Просмотр аналитики

```
1. Открыть Mini App → Аналитика
2. Выбрать период "Месяц"
3. График загружается
4. Pie chart категорий
5. Экспорт в CSV
```

### Сценарий 4: Установка бюджета

```
1. Отправить боту: "Установи бюджет 10000 сом на месяц"
2. Бот сохраняет бюджет
3. Открыть Mini App → статус бюджета
4. Проверить процент выполнения
```

---

## 🐛 СПИСОК ИЗВЕСТНЫХ БАГОВ

### ✅ Исправлены:

- [x] Дублирование daily отчётов (v2.4.5)
- [x] Неправильный период в monthly отчёте (v2.4.4)
- [x] Cache invalidation (v3.0.3)

### 🔴 Активные баги:

- [ ] (пока нет известных)

---

## 📊 МЕТРИКИ ДЛЯ МОНИТОРИНГА

### Backend:

```
✅ Response time < 200ms
✅ Error rate < 1%
✅ Uptime > 99.5%
✅ Database connections < 50
```

### Frontend:

```
✅ Load time < 2s
✅ Time to Interactive < 3s
✅ Cache hit rate > 70%
✅ API errors < 5%
```

### Database:

```
✅ Connection pool < 80% full
✅ Query time < 100ms (avg)
✅ Disk usage < 80%
✅ No deadlocks
```

---

## 🚀 ДЕПЛОЙ ЧЕКЛИСТ

### Перед деплоем:

- [ ] Обновить версию в `app.js` (APP_VERSION)
- [ ] Обновить версию в `sw.js` (CACHE_NAME)
- [ ] Обновить `CHANGELOG.md`
- [ ] Тестирование локально
- [ ] Проверить .env переменные

### Деплой Backend (Railway):

```bash
1. Push в GitHub
2. Railway auto-deploy
3. Проверить логи
4. Тест /health endpoint
5. Тест авторизации
```

### Деплой Frontend (Cloudflare Pages):

```bash
1. Push в GitHub
2. Cloudflare auto-deploy
3. Очистить CF cache
4. Проверить загрузку
5. Тест функционала
```

### После деплоя:

- [ ] Smoke test всех эндпоинтов
- [ ] Проверить Mini App в Telegram
- [ ] Проверить n8n workflows
- [ ] Мониторинг логов 30 минут
- [ ] Уведомить пользователей (если breaking changes)

---

## 📈 ПЛАН РАЗВИТИЯ (ROADMAP)

### Q1 2026:

- [ ] Добавить Redis для кэширования
- [ ] Реализовать WebSocket
- [ ] Push уведомления в Mini App
- [ ] Cursor pagination
- [ ] Rate limiting

### Q2 2026:

- [ ] Пользовательские категории
- [ ] Теги для транзакций
- [ ] Recurring expenses
- [ ] Savings goals
- [ ] PDF отчёты в Mini App

### Q3 2026:

- [ ] ML предсказание расходов
- [ ] Fraud detection
- [ ] OCR для чеков
- [ ] Мультиязычность (EN, KG)
- [ ] Экспорт в 1C

### Q4 2026:

- [ ] Mobile apps (iOS, Android)
- [ ] Web версия (не в Telegram)
- [ ] API для третьих сторон
- [ ] Premium функции
- [ ] Маркетплейс интеграций

---

## 📞 КОНТАКТЫ БЫСТРОГО ДОСТУПА

### Сервисы:

- **Railway:** https://railway.app/dashboard
- **Cloudflare:** https://dash.cloudflare.com
- **Supabase:** https://supabase.com/dashboard
- **n8n:** https://hi9neee.app.n8n.cloud
- **GitHub:** https://github.com/hi9ne/AIAccounter

### Логи:

- **Backend:** Railway Dashboard → Deployments → Logs
- **Frontend:** Cloudflare Pages → Deployments → Logs
- **Database:** Supabase → Database → Logs
- **n8n:** Executions tab

---

## 🎓 ПОЛЕЗНЫЕ КОМАНДЫ

### Backend (локально):

```bash
# Запуск
uvicorn app.main:app --reload --port 8000

# Миграции
alembic revision --autogenerate -m "description"
alembic upgrade head

# Тесты
pytest

# Линтинг
flake8 app/
black app/
```

### Frontend (локально):

```bash
# Запуск
python -m http.server 5500
# или
npx serve -p 5500

# Проверка
npm run lint  # если добавлено
```

### Database:

```bash
# Подключение к Supabase
psql postgresql://postgres:password@host:5432/postgres

# Бэкап
pg_dump postgresql://... > backup.sql

# Восстановление
psql postgresql://... < backup.sql
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Проверить текущий деплой
2. ✅ Убедиться что всё работает
3. 🔄 Определить приоритетные улучшения
4. 📝 Создать задачи в GitHub Issues
5. 🚀 Начать разработку

---

**Последнее обновление:** 21 ноября 2025  
**Автор:** hi9ne  
**Статус:** 🟢 Готов к работе
