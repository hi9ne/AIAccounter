# Исправление: Автоматическое использование workspace и валюты

## Проблема

Бот **спрашивал** пользователя указать workspace и валюту при добавлении операций:

```
Пользователь: "Прибыль 5000 с клиента"
Бот: "Пожалуйста, уточните, в каком рабочем пространстве вы хотите записать 
     этот доход, а также укажите валюту (если это не сом)."
```

**Проблемы:**
1. Workspace уже определяется автоматически через SQL JOIN
2. Валюта пользователя сохранена в профиле (`preferred_currency` из анкеты)
3. AI промпт требовал указывать workspace_id вручную
4. Лишние шаги - плохой UX

## Решение

### 1. Упрощен промпт AI ✅

**Файл:** `AnaliziFinance.json`, строка ~312

**Было:**
```
🏢 СИСТЕМА WORKSPACE:
У каждого пользователя есть workspace_id. Все операции привязаны к workspace. 
Используй функции: get_user_workspaces(), create_workspace_with_owner()...

💸 РАСХОДЫ: используй инструмент Add_expense с параметрами:
- workspace_id: ID рабочего пространства (ОБЯЗАТЕЛЬНО!)
- currency: валюта (KGS/сом по умолчанию...)
```

**Стало:**
```
👤 ВАЛЮТА ПОЛЬЗОВАТЕЛЯ: {{ $json.preferred_currency || 'KGS' }}

⚙️ ВАЖНЫЕ ПРАВИЛА:
1. Workspace определяется АВТОМАТИЧЕСКИ - НЕ спрашивай пользователя
2. Если валюта НЕ указана в сообщении - используй валюту пользователя
3. СРАЗУ вызывай инструмент, НЕ спрашивай подтверждений
4. НЕ спрашивай "укажите валюту" - используй валюту пользователя

Примеры:
- "Прибыль 5000 с клиента" → СРАЗУ вызов Add_income с валютой пользователя
- "Потратил 200 на такси" → СРАЗУ вызов Add_expense с валютой пользователя

💸 РАСХОДЫ: Add_expense
- amount: число
- currency: если НЕ указана - используй валюту пользователя
- category: продукты, транспорт, кафе/рестораны...
```

### 2. Добавлена валюта пользователя в контекст ✅

**Файл:** `AnaliziFinance.json`, строка ~540

Узел "Merge Onboarding Complete" теперь передаёт:
```json
{
  "assignments": [
    {
      "name": "message",
      "value": "={{ $('Edit Fields').first().json.message }}"
    },
    {
      "name": "preferred_currency",
      "value": "={{ $('Check Onboarding Status').first().json.preferred_currency || 'KGS' }}"
    }
  ]
}
```

Теперь AI видит валюту пользователя: `{{ $json.preferred_currency }}`

### 3. Убраны упоминания workspace_id для пользователей ✅

- Workspace определяется **автоматически** через SQL:
  ```sql
  WITH user_workspace AS (
    SELECT w.id as workspace_id
    FROM workspaces w
    JOIN workspace_members wm ON w.id = wm.workspace_id
    WHERE wm.user_id = {{ user_id }}
      AND wm.is_active = true
    ORDER BY wm.joined_at DESC
    LIMIT 1
  )
  ```

- AI больше НЕ требует workspace_id
- Пользователь НЕ видит упоминания "workspace"

## Как это работает

### До исправления ❌

```
Пользователь: "Прибыль 5000 с клиента"
  ↓
AI читает промпт: "workspace_id: ID рабочего пространства (ОБЯЗАТЕЛЬНО!)"
  ↓
AI: "Недостаточно информации - нужен workspace"
  ↓
Бот: "Пожалуйста, уточните, в каком рабочем пространстве..."
  ↓
Пользователь: ❌ Раздражен, не понимает что такое workspace
```

### После исправления ✅

```
Пользователь: "Прибыль 5000 с клиента"
  ↓
AI читает промпт: "ВАЛЮТА ПОЛЬЗОВАТЕЛЯ: KGS"
AI читает промпт: "Workspace определяется АВТОМАТИЧЕСКИ"
AI читает промпт: "СРАЗУ вызывай инструмент, НЕ спрашивай"
  ↓
AI вызывает: Add_income(amount: 5000, currency: 'KGS', category: 'продажи')
  ↓
SQL автоматически определяет workspace_id через JOIN
  ↓
Бот: "✅ Доход добавлен: 5000 KGS на 'продажи' (05.11.2025)"
  ↓
Пользователь: ✅ Доволен, всё работает просто
```

## Преимущества

1. **Простота** - пользователь НЕ видит технические детали (workspace)
2. **Скорость** - нет лишних вопросов, операция добавляется сразу
3. **Умная валюта** - автоматически используется валюта из анкеты
4. **Гибкость** - можно явно указать другую валюту: "Получил 100 долларов"

## Тестирование

### Тест 1: Доход без валюты
```
Пользователь: "Прибыль 5000 с клиента"
Ожидается: ✅ Доход добавлен: 5000 KGS на 'продажи'
```

### Тест 2: Расход без валюты
```
Пользователь: "Потратил 300 на такси"
Ожидается: ✅ Расход добавлен: 300 KGS на 'транспорт'
```

### Тест 3: Доход с явной валютой
```
Пользователь: "Получил 100 долларов зарплата"
Ожидается: ✅ Доход добавлен: 100 USD на 'зарплата'
```

### Тест 4: Проверка workspace
```sql
-- Убедиться, что operations записались в правильный workspace
SELECT 
  i.id,
  i.amount,
  i.currency,
  i.workspace_id,
  w.name as workspace_name
FROM income i
JOIN workspaces w ON i.workspace_id = w.id
WHERE i.user_id = 1109421300
ORDER BY i.created_at DESC
LIMIT 5;
```

## Технические детали

### Поток данных

```
Telegram Message
  ↓
Edit Fields (добавляет user_id)
  ↓
Check Onboarding Status (получает профиль: preferred_currency, onboarding_completed)
  ↓
Onboarding Router (проверяет onboarding_completed)
  ↓ (TRUE)
Merge Onboarding Complete (передаёт: message + preferred_currency)
  ↓
Message Type Switch
  ↓
Financial Assistant (получает: message, preferred_currency)
  ↓
AI видит в промпте: "ВАЛЮТА ПОЛЬЗОВАТЕЛЯ: {{ $json.preferred_currency }}"
  ↓
Add_income / Add_expense (SQL автоматически получает workspace_id)
```

### SQL - автоматическое определение workspace

```sql
-- В Add_income и Add_expense
WITH user_workspace AS (
  SELECT w.id as workspace_id
  FROM workspaces w
  JOIN workspace_members wm ON w.id = wm.workspace_id
  WHERE wm.user_id = {{ user_id }}
    AND wm.is_active = true
    AND w.is_active = true
  ORDER BY wm.joined_at DESC  -- последний joined workspace
  LIMIT 1
)
INSERT INTO income (user_id, workspace_id, ...)
SELECT {{ user_id }}, uw.workspace_id, ...
FROM user_workspace uw
```

**Логика:**
1. Находит **активный** workspace пользователя
2. Сортирует по дате присоединения (DESC) - берёт последний
3. LIMIT 1 - только один workspace
4. Если у пользователя несколько workspace - используется последний

## Будущие улучшения

### Фаза 1 (текущая) - Автоматический workspace ✅
- У каждого пользователя автоматически создаётся личный workspace при регистрации
- Все операции идут в этот workspace
- Пользователь НЕ видит термин "workspace"

### Фаза 2 (будущее) - Приглашения
- Пользователь может создать новый workspace: "Создай workspace 'Семейный бюджет'"
- Можно пригласить других: "Добавь @username в workspace"
- Переключение между workspace: "Переключись на 'Семейный бюджет'"
- Настройка прав: owner, admin, editor, viewer

### Фаза 3 (будущее) - Умное определение
- AI анализирует контекст: "Добавь 1000 на продукты для семейного бюджета"
- Автоматически определяет workspace по ключевым словам
- История: запоминает последний используемый workspace

## Файлы изменены

- ✅ `AnaliziFinance.json`:
  - Financial Assistant prompt (строка ~312) - упрощён и добавлена валюта
  - Merge Onboarding Complete (строка ~540) - передаёт preferred_currency

## Связанные проблемы

- [x] Бот спрашивал workspace при каждой операции
- [x] Бот спрашивал валюту даже если она в профиле
- [x] AI игнорировал автоматическое определение workspace в SQL
- [x] Плохой UX - лишние вопросы

## Статус: ✅ ИСПРАВЛЕНО

Дата: 2025-11-05  
Версия: v2.4.3  
Приоритет: HIGH (UX)
