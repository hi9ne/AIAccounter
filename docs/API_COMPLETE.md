# 🚀 AIAccounter Backend API - Полная документация

**Версия:** v2.5  
**Дата:** 12.11.2025  
**Статус:** ✅ Все эндпоинты созданы и готовы к тестированию

---

## 📋 Содержание

1. [Аутентификация](#1-аутентификация)
2. [Пользователи](#2-пользователи)
3. [Категории](#3-категории)
4. [Workspaces](#4-workspaces)
5. [Курсы валют](#5-курсы-валют)
6. [Аналитика](#6-аналитика)
7. [Отчёты](#7-отчёты)
8. [Расходы](#8-расходы)
9. [Доходы](#9-доходы)
10. [Бюджеты](#10-бюджеты)

---

## 1. Аутентификация

**Базовый URL:** `/api/v1/auth`

### POST `/telegram`
Авто-регистрация/вход через Telegram Mini App
- **Body:** `TelegramAuthData`
- **Response:** JWT токен
- **Особенность:** Создаёт пользователя автоматически при первом входе

### GET `/me`
Получить информацию о текущем пользователе
- **Auth:** Bearer Token
- **Response:** Данные пользователя

---

## 2. Пользователи

**Базовый URL:** `/api/v1/users`  
**Auth:** Все эндпоинты требуют JWT

### GET `/me`
Профиль текущего пользователя

### PUT `/me`
Обновить профиль
- **Body:** `UserUpdate` (first_name, last_name, language, timezone)

### GET `/me/stats`
Детальная статистика пользователя
- **Использует:** PostgreSQL функцию `get_user_profile()`
- **Возвращает:** 
  - Общие расходы/доходы
  - Баланс
  - Количество транзакций
  - Средний расход
  - Топ категория

---

## 3. Категории

**Базовый URL:** `/api/v1/categories`  
**Auth:** JWT требуется

### GET `/expenses`
35 категорий расходов с эмодзи

### GET `/income`
15 категорий доходов с эмодзи

### GET `/currencies`
5 поддерживаемых валют (KGS, USD, EUR, RUB, KZT)

### GET `/all`
Все категории + валюты одним запросом

---

## 4. Workspaces

**Базовый URL:** `/api/v1/workspaces`  
**Auth:** JWT требуется

### POST `/`
Создать новый workspace
- **Body:** `WorkspaceCreate` (name, description, currency)
- **Использует:** `create_workspace_with_owner()`

### GET `/`
Список всех workspaces пользователя
- **Использует:** `get_user_workspaces()`

### GET `/{workspace_id}`
Детали workspace
- **Проверка:** `check_workspace_permission(viewer)`

### PUT `/{workspace_id}`
Обновить workspace
- **Права:** Admin/Owner
- **Body:** `WorkspaceUpdate`

### DELETE `/{workspace_id}`
Удалить workspace
- **Права:** Только Owner

### GET `/{workspace_id}/members`
Список участников

### DELETE `/{workspace_id}/members/{user_id}`
Удалить участника
- **Права:** Admin/Owner
- **Использует:** `remove_workspace_member()`

### POST `/{workspace_id}/invites`
Создать приглашение
- **Права:** Admin
- **Body:** `WorkspaceInviteCreate`

### POST `/{workspace_id}/invites/{invite_id}/accept`
Принять приглашение
- **Использует:** `accept_workspace_invite()`

### GET `/invites/pending`
Мои ожидающие приглашения

---

## 5. Курсы валют

**Базовый URL:** `/api/v1/rates`  
**Auth:** JWT требуется

### GET `/latest`
Актуальные курсы валют
- **Query:** `from_currency`, `to_currency` (опционально)

### GET `/history`
История курсов
- **Query:** `from_currency`, `to_currency`, `start_date`, `end_date`

### GET `/{from_currency}/{to_currency}`
Курс для конкретной пары
- **Query:** `rate_date` (опционально)

### POST `/convert`
Конвертация валюты
- **Body:** `ConversionRequest` (from_currency, to_currency, amount)
- **Особенность:** Автоматически использует обратный курс если прямой не найден

### POST `/`
Добавить новый курс
- **Body:** `ExchangeRateCreate`

### GET `/currencies/supported`
Список поддерживаемых валют

---

## 6. Аналитика

**Базовый URL:** `/api/v1/analytics`  
**Auth:** JWT требуется

### GET `/stats`
Базовая статистика доходов/расходов
- **Query:** `workspace_id`, `start_date`, `end_date`
- **Использует:** `get_income_expense_stats()`

### GET `/categories/top`
Топ категорий расходов
- **Query:** `workspace_id`, `start_date`, `end_date`, `limit` (по умолчанию 10)
- **Использует:** `get_top_categories()`

### GET `/chart/balance-trend`
Тренд баланса по дням
- **Query:** `workspace_id`, `start_date`, `end_date`
- **Использует:** `get_balance_trend()`

### GET `/chart/income-expense`
Данные для графика доходов/расходов
- **Query:** `workspace_id`, `start_date`, `end_date`, `group_by` (day/week/month)
- **Использует:** `get_income_expense_chart_data()`

### GET `/chart/category-pie`
Круговая диаграмма по категориям
- **Query:** `workspace_id`, `start_date`, `end_date`, `transaction_type`
- **Использует:** `get_category_pie_chart_data()`

### GET `/patterns`
Паттерны трат (регулярные платежи)
- **Query:** `workspace_id`
- **Использует:** `get_spending_patterns()`

### GET `/dashboard`
Все данные для дашборда одним запросом
- **Query:** `workspace_id`, `period` (week/month/year)
- **Возвращает:** stats + top_categories + balance_trend

### GET `/compare-periods`
Сравнить два периода
- **Query:** `workspace_id`, `period1_start`, `period1_end`, `period2_start`, `period2_end`
- **Возвращает:** Статистика + изменения в %

---

## 7. Отчёты

**Базовый URL:** `/api/v1/reports`  
**Auth:** JWT требуется  
**Технология:** APITemplate.io

### POST `/weekly`
Недельный PDF отчёт
- **Query:** `workspace_id`, `week_start` (опционально)
- **Возвращает:** URL для скачивания PDF
- **Template:** Использует `WEEKLY_TEMPLATE_ID`

### POST `/monthly`
Месячный PDF отчёт + бюджеты
- **Query:** `workspace_id`, `year`, `month` (опционально)
- **Возвращает:** URL для скачивания PDF
- **Template:** Использует `MONTHLY_TEMPLATE_ID`
- **Особенность:** Включает сравнение с бюджетами

### POST `/period`
Отчёт за произвольный период
- **Body:** `ReportRequest` (workspace_id, start_date, end_date, include_transactions)
- **Возвращает:** URL для скачивания PDF
- **Template:** Использует `PERIOD_TEMPLATE_ID`
- **Особенность:** Может включать все транзакции

### GET `/history`
История сгенерированных отчётов
- **Query:** `workspace_id`, `limit`
- **Статус:** Coming soon (требует таблицу report_history)

---

## 8. Расходы

**Базовый URL:** `/api/v1/expenses`  
**Auth:** JWT требуется

### POST `/`
Создать расход

### GET `/`
Список расходов

### GET `/{id}`
Получить расход

### PUT `/{id}`
Обновить расход

### DELETE `/{id}`
Удалить расход

### GET `/stats/summary`
Сводная статистика

### GET `/stats/by-category`
Статистика по категориям

---

## 9. Доходы

**Базовый URL:** `/api/v1/income`  
**Auth:** JWT требуется

### POST `/`
Создать доход

### GET `/`
Список доходов

### GET `/{id}`
Получить доход

### PUT `/{id}`
Обновить доход

### DELETE `/{id}`
Удалить доход

---

## 10. Бюджеты

**Базовый URL:** `/api/v1/budget`  
**Auth:** JWT требуется

### POST `/`
Создать бюджет

### GET `/`
Список бюджетов

### GET `/{id}`
Получить бюджет

### PUT `/{id}`
Обновить бюджет

### DELETE `/{id}`
Удалить бюджет

---

## 🔧 Настройка

### 1. Переменные окружения (.env)

```bash
# Database
DATABASE_URL=postgresql://...

# Security
SECRET_KEY=your-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# APITemplate.io
APITEMPLATE_API_KEY=your-apitemplate-api-key
WEEKLY_TEMPLATE_ID=your-weekly-template-id
MONTHLY_TEMPLATE_ID=your-monthly-template-id
PERIOD_TEMPLATE_ID=your-period-template-id
```

### 2. Запуск

```bash
cd backend
python -m venv venv
.\venv\Scripts\activate  # Windows
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### 3. Документация API

После запуска:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

---

## 🎯 Особенности реализации

### JWT Аутентификация
- Все эндпоинты (кроме /auth/telegram) защищены JWT
- Автоматическая регистрация через Telegram Mini App
- Токены действительны 30 минут

### PostgreSQL Functions
Активно используются готовые функции БД:
- `get_user_profile()` - детальная статистика пользователя
- `create_workspace_with_owner()` - создание workspace с owner
- `check_workspace_permission()` - проверка прав доступа
- `get_income_expense_stats()` - статистика доходов/расходов
- `get_top_categories()` - топ категорий
- `get_balance_trend()` - тренд баланса
- `get_spending_patterns()` - паттерны трат

### APITemplate.io Integration
- Генерация PDF отчётов через внешний сервис
- Готовые HTML шаблоны в `pdf-templates/`
- Минималистичный дизайн с ApexCharts

---

## ✅ Статус TODO

- [x] JWT аутентификация
- [x] Дополнить модели SQLAlchemy
- [x] User Profile API
- [x] Workspaces API
- [x] Categories API
- [x] Exchange Rates API
- [x] Analytics API
- [x] Reports API

**Все задачи выполнены!** 🎉

---

## 🧪 Тестирование

### Порядок тестирования:

1. **Аутентификация**
   ```bash
   POST /api/v1/auth/telegram
   # Получить токен
   ```

2. **Создать workspace**
   ```bash
   POST /api/v1/workspaces
   ```

3. **Добавить транзакции**
   ```bash
   POST /api/v1/expenses
   POST /api/v1/income
   ```

4. **Получить аналитику**
   ```bash
   GET /api/v1/analytics/dashboard?workspace_id=1&period=month
   ```

5. **Сгенерировать отчёт**
   ```bash
   POST /api/v1/reports/monthly?workspace_id=1
   ```

---

## 📝 Примечания

- **Все эндпоинты созданы** - код написан, зависимости установлены
- **Требуется настройка** APITemplate.io для работы PDF отчётов
- **База данных** использует существующие функции и таблицы
- **Готов к деплою** на любой платформе (Railway, Render, AWS, etc.)

---

**Разработчик:** GitHub Copilot  
**Проект:** AIAccounter v2.5  
**Дата:** 12.11.2025
